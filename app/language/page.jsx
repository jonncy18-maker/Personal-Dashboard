import ComingSoon from '../../components/ComingSoon';

export default function LanguagePage() {
  return (
    <ComingSoon
      domain="language"
      note="Mostly a placeholder for now — the next Spanish tutor call already reads live from Calendar on Home."
    />
  );
}
