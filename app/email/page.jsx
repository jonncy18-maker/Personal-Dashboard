import ComingSoon from '../../components/ComingSoon';

export default function EmailPage() {
  return (
    <ComingSoon
      domain="email"
      note="Read-only Gmail triage with Tier 1 + Tier 2 hide rules and the first-run onboarding scan land here next."
    />
  );
}
