import ComingSoon from '../../components/ComingSoon';

export default function SchedulesPage() {
  return (
    <ComingSoon
      domain="schedules"
      note="The cross-domain task list — title, notes, due date, optional trip/project link — lands here next."
    />
  );
}
