# API Keys

The full key table for this project. The security rules themselves (server-side-only access, no `NEXT_PUBLIC_` on secrets, Gmail read-only) live in `CLAUDE.md` §2.

| Key                                                    | Prefix             | Lives                                           | Why                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------ | ------------------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Neon connection string (`DATABASE_URL`)                | none (server-only) | Vercel env (Production + Preview)               | DB access — never exposed to browser                                                                                                                                                                                                                                            |
| Vercel API token                                       | none (server-only) | Vercel env (Production + Preview)               | Read-only calls to Vercel's own API (`list_projects`, `get_project`, `list_deployments`) for AI Projects' live deploy status                                                                                                                                                    |
| GitHub API access (`GITHUB_TOKEN`)                     | none (server-only) | Vercel env (Production + Preview), **optional** | Public repo reads work with no token. Set `GITHUB_TOKEN` to raise the rate limit (60→5000 req/hr) and reach private repos. Used by `lib/github.js` for AI Projects (repo meta, commits, milestones, Next Up)                                                                    |
| Google OAuth (`GOOGLE_CLIENT_ID/SECRET/REFRESH_TOKEN`) | none (server-only) | Vercel env (Production + Preview)               | One client, read-only scopes for **Calendar** (next Spanish tutor call) and **Gmail** (Email domain + Travel import)                                                                                                                                                            |
| Anthropic API key (Claude Haiku)                       | none (server-only) | Vercel env (Production + Preview)               | Narrow uses only: Email Tier 2 semantic residual, Travel itinerary parse, Language's French hours screenshot import. See the `email`, `travel`, and `language` skills (Travel's AI Brief used this too — retired 2026-09-07)                                                                                              |
| Unsplash access key (`UNSPLASH_ACCESS_KEY`)            | none (server-only) | Vercel env (Production + Preview)               | Read-only search API — auto-fetches a trip's destination photo (Travel, auto + manual override). See the `travel` skill                                                                                                                                                                         |
| Google Geocoding API key (`GOOGLE_MAPS_API_KEY`)       | none (server-only) | Vercel env (Production + Preview)               | Forward/reverse geocoding for Travel's trip map + country stats and Mileage's places/trips/legs (`lib/geocode.js`). Switched from free Nominatim 2026-08-26 — real coverage gaps on rural addresses; Google's free monthly credit comfortably covers single-user volume. See the `geocoding` skill |
